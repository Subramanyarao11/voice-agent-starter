name: sahaayak

services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: sahaayak
      POSTGRES_PASSWORD: sahaayak
      POSTGRES_DB: sahaayak
    ports:
      # Published on a non-default host port so the stack coexists with a
      # native Postgres on 5432. Containers reach this by service name on the
      # compose network, so the mapping only affects tools run on the host.
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U sahaayak"]
      interval: 5s
      timeout: 3s
      retries: 10

  redis:
    image: redis:7
    ports:
      # Likewise offset, so a native redis-server on 6379 is left alone.
      - "6380:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10

  api:
    build:
      context: .
      dockerfile: infra/python/Dockerfile
    env_file: .env
    environment:
      DATABASE_URL: postgresql+psycopg://sahaayak:sahaayak@postgres:5432/sahaayak
      REDIS_URL: redis://redis:6379/0
      ADMIN_API_TOKEN: ${ADMIN_API_TOKEN:-}
      ADMIN_TOKENS_JSON: ${ADMIN_TOKENS_JSON:-}
      ADMIN_OIDC_ENABLED: ${ADMIN_OIDC_ENABLED:-}
    ports:
      - "8000:8000"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    volumes:
      - ./packages:/app/packages
      - ./services:/app/services
      - ./scripts:/app/scripts
    command: >
      uvicorn sahaayak_api.main:app --host 0.0.0.0 --port 8000 --reload

  # Separate process on purpose: a citizen waiting on an API response must
  # never be queued behind a messaging provider that is timing out. Harmless
  # to run with every channel disabled — it claims nothing.
  notification-worker:
    build:
      context: .
      dockerfile: infra/python/Dockerfile
    env_file: .env
    environment:
      DATABASE_URL: postgresql+psycopg://sahaayak:sahaayak@postgres:5432/sahaayak
      REDIS_URL: redis://redis:6379/0
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    volumes:
      - ./packages:/app/packages
      - ./services:/app/services
      - ./scripts:/app/scripts
    command: >
      python scripts/14_run_notification_worker.py --forever

volumes:
  pgdata:
